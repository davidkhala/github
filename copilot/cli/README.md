# `copilot`
[source](https://github.com/github/copilot-cli)

Install by npm
- stable: `npm install -g @github/copilot`
- next: `npm install -g @github/copilot@prerelease`

# usage
Greeting banner: `copilot --banner`

> Run the `/model` command to choose from other available models
